version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: docker run -d -p 8545 --name ganache stevenzeiler/ganache
      - run: docker build -t named-robot .
      - run: ./run_tests_docker.sh
